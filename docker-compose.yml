services:
  web:
    image: nginx:latest
    ports:
      - "${WEB_PORT}:80"  # .env에서 읽음

  db:
    image: postgres:13-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}